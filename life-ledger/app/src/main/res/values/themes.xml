<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The splash theme exists only to hand off to Compose without a white flash; every
      other surface in the app is themed by LifeLedgerTheme in :core:designsystem.
    -->
    <style name="Theme.LifeLedger.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.LifeLedger</item>
    </style>

    <style name="Theme.LifeLedger" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
