<?xml version="1.0" encoding="utf-8"?>
<!--
  Life Ledger opts out of cloud backup and device-to-device transfer entirely. The
  database is the user's only copy of years of history, and the app's own encrypted
  export is the supported way to move it — a path where the user holds the passphrase
  rather than a platform account holding the plaintext.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
